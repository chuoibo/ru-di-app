# qa2-014654 — đo lại 47 tính năng sau #388/#379, và câu hỏi điều hướng ở **cửa Google**

- **Đo trên**: `main` tại `43dc45a`
- **Máy chủ**: stack dùng một lần **của riêng lượt này**, dựng từ chính `43dc45a`
  (`scripts/e2e_slice.sh --keep`) — `http://127.0.0.1:48045`, PostgreSQL riêng,
  không đụng `mobile-local-api-1` ở `:8099`
- **Bundle web**: `expo export --platform web --clear` từ `43dc45a`,
  `EXPO_PUBLIC_API_URL=http://127.0.0.1:48045`, phục vụ ở `127.0.0.1:8946`
- **Trình duyệt**: Playwright Chromium, khung 390×844, `--no-proxy-server`,
  mở `/` **không fragment, không query**
- **Kỹ năng đã gọi**: `e2e-testing`
- **protocol_version**: v1 · **Ngày**: 2026-08-31

Đây là **kiểm kê hành vi**, không phải phán quyết PR. Không có verdict
`APPROVE` / `REQUEST_CHANGES` / `REJECT` trong tài liệu này.

Nền so sánh: `docs/archive/claude/2026-08-31/qa2-002802-dem-lai-47-tinh-nang.md` — đo trên
`1161570`, ra **32/47**, và để lại đúng một hàng "đo lại đầu tiên" là F16.

---

## 0. Hai câu trả lời

### Câu 1 — vào bằng **cửa Google** thì có điều hướng không?

**Có. Đo tại chính cửa Google, trên bundle `43dc45a`.** Lượt trước tôi trả lời
"có" nhưng con số dán kèm là đo ở **cửa số điện thoại**; lần này đi đúng cửa
leader hỏi: bấm "Đăng ký với Google" → danh sách người của nhóm demo → chọn
"Vào app với tư cách Minh".

Đếm **mọi vai trò tương tác**, không chỉ `button` — đúng bài học leader ghi lại
lúc 01:59:

```
màn mở đầu (chưa đăng nhập):
  {"button":4,"tab":0,"tablist":0,"link":0,"menuitem":0,"switch":0,
   "checkbox":0,"radio":0,"input":0,"select":0}   innerText 276 ký tự

sau khi vào bằng cửa GOOGLE:
  {"button":8,"tab":4,"tablist":1,"link":0,"menuitem":0,"switch":0,
   "checkbox":0,"radio":5,"input":1,"select":0}   innerText 713 ký tự
```

Bốn tab thật, cùng `y=788`, ngay khi vào shell:

```
[788] tab    "Khám phá: gợi ý chỗ đi cho nhóm"
[788] tab    "Lên plan: chuyến đi của nhóm"
[788] tab    "Tin nhắn: chat nhóm và AI"
[788] tab    "Cá nhân: hồ sơ và tài chính của bạn"
[766] button "Tạo mới"                       ← nút [+], mở sheet 5 mục
```

Nên câu trả lời là **cả hai cửa đều có điều hướng**, và cửa Google giờ có số đo
của chính nó chứ không phải suy từ cửa bên cạnh.

Một chi tiết phụ đáng ghi: ba nút ở màn mở đầu (**Google · Apple · số điện
thoại**) **không có `aria-label`**, chỉ có chữ hiện. Đọc được bằng mắt, nhưng
máy quét nào lọc theo nhãn sẽ không thấy chúng. Không phải blocker, là ghi chú
cho lượt a11y.

### Câu 2 — con số, ghi rõ **cửa vào**

| Vào bằng cửa nào | BẤM-ĐƯỢC | TẮC | KHÔNG-CÓ-ĐƯỜNG |
|---|---|---|---|
| **Google** → chọn Minh trong Team Đà Lạt | **35 / 47** | 4 | 8 |
| **Số điện thoại** → tài khoản mới tinh | chưa đo lại lượt này — xem mục 5 | — | — |
| **"Bỏ qua"** (vào shell với `nguoi = null`) | chưa đo hàng nào | — | — |

`30 (ba510d8) → 32 (1161570) → 35 (43dc45a)`. Ba hàng đổi nhãn, **không hàng nào tụt**.

**Đọc chặt hơn thì là 34**: F37 tới được và máy chủ trả `200`, nhưng nhóm không
có tấm ảnh nào nên **chưa có thước phim nào thật sự được dựng**. Ai muốn con số
"tính năng chạy trọn vẹn" thì trừ hàng đó ra; ai đếm "đường bấm tới nơi và máy
chủ trả lời" thì giữ. Tôi ghi cả hai chứ không chọn hộ.

---

## 1. Ba hàng đổi nhãn — và vì sao chỉ ba hàng này có thể đổi

`git diff 1161570..43dc45a` chạm đúng 10 file sản phẩm:

```
apps/mobile/src/navigation/{AppRoot,MenuTao,VoTab,icons}.tsx · {lien-ket,tabs}.ts
apps/mobile/src/screens/album/{AlbumChuyenDi.tsx,album-api.ts}
services/api/app/api/companion_gemini.py · app/domain/companion.py
```

Không màn nào khác bị chạm, nên chỉ F16 (companion), F36/F37 (album) và điều
hướng có thể đổi. Cả ba đều được **đi bằng cách bấm** lượt này, không suy từ grep.

### F16 — TẮC → **BẤM-ĐƯỢC** (đây là hàng #388 vá)

Lượt trước F16 chết ở **cổng nhịp** (`asked_too_often`), nên tôi không đo được
đường lịch trình. Lượt này, trong một nhóm còn nguyên hạn mức: gõ ba tin như một
nhóm bàn thật, rồi bấm "Hỏi Rủ Đi AI".

```
POST 200 /contexts/{id}/ai-turn
body: {"spoke": true, "reason": "ok",
       "message": {"kind":"ai_card","card":{"kind":"itinerary",
         "payload":{"title":"Gợi ý lịch trình Đà Lạt 2 ngày cuối tuần",
                    "stops":[6 chặng, mọi place_id nằm trong catalogue]}}}}
```

Trên màn: thẻ "Gợi ý lịch trình Đà Lạt 2 ngày cuối tuần" với *Sáng ngày 1 · Lưng
Chừng Cafe*, *Chiều ngày 1 · Khu vui chơi DREAMpark*, *Tối ngày 1 · Lẩu Gà Lá É
Tao Ngộ*, và nút **"Xem chi tiết kế hoạch"** mở màn đầy đủ 6 chặng kèm địa chỉ,
khoảng giá và dòng "Khoảng giá là của từng chỗ, do máy chủ gửi. App không cộng."

**Cái tôi KHÔNG chứng minh**: rằng chính `c5c74d5` làm hàng này sống lại. Tôi
không chạy A/B với bản trước vá — bằng chứng đó nằm trong PR #388 (10/10 REFUSED
trước, 10/10 grounded sau). Cái tôi chứng minh là: **trên máy chủ đã có bản vá,
người dùng bấm được từ chat ra một lịch trình đầy đủ.**

Máy demo `:8099` **chưa có bản vá** (`grep -c _optional_text /srv/app/domain/companion.py`
trong container → `0`), nên ai đo F16 ở đó vẫn sẽ thấy hàng cũ.

### F36 — KHÔNG-CÓ-ĐƯỜNG → **BẤM-ĐƯỢC**

Đường bấm ba tầng, mỗi tầng sinh đúng một lời gọi thật:

```
[+] "Tạo mới" → sheet 5 mục, có "Album chuyến đi. Từng chuyến một: ảnh, chỗ đã tới, tiền, và thước phim"
  GET 200 /contexts/{id}/albums                       → "Nhóm đã có 3 album"
  GET 200 /contexts/{id}/albums/{outing_id}           → "Bữa nướng cuối tuần"
  GET 200 /contexts/{id}/albums/{outing_id}/reel      → màn Thước phim AI
```

Kệ album in tiền đọc từ sổ: *Bữa nướng cuối tuần 960.000đ · Chuyến Đà Lạt tháng 8
1.625.000đ · Chuyến Đà Lạt tháng 6 4.200.000đ*, khớp bộ dữ liệu seed.

### F37 — KHÔNG-CÓ-ĐƯỜNG → **BẤM-ĐƯỢC (rỗng)**

Nút "Dựng thước phim" có thật, `GET 200 …/reel` có thật, và màn nói thẳng:

```
Chưa dựng được thước phim
Chuyến này chưa có tấm ảnh nào trong nhóm, nên chưa có gì để dựng thành thước phim.
Máy chủ trả lời bình thường, chỉ là không có bản dựng nào. Màn này không thay bằng
một thước phim cũ hay một thước phim viết sẵn.
```

Trạng thái rỗng **trung thực** — nó không bịa. Nhưng "AI dựng được thước phim từ
ảnh thật" thì lượt này **chưa đo được**: cả stack riêng của tôi lẫn nhóm demo
chung đều có **0 ảnh** (`select count(*) from posts where image_url is not null` → `0`).
Đây là ô chưa quét, không phải hàng đã xong.

---

## 2. Bằng chứng phép đo còn sống

```
CANARY SẠCH             : button 4 · tab 4 · tablist 1 · innerText 276→713 ký tự
CANARY XẤU (chặn **/*.js): mọi vai trò = 0 · innerText 0 ký tự
```

Hai số khác nhau → trình duyệt thật sự nạp và chạy bundle. Bằng nhau thì cả tài
liệu này phải vứt.

Hash phục vụ **==** hash dựng:

```
curl http://127.0.0.1:8946/index.html → index-645c782f14425fa8f224f7d91411bcc4.js
ls  /tmp/qa2-dist-sau379/_expo/static/js/web/ → index-645c782f14425fa8f224f7d91411bcc4.js
```

Máy chủ đúng là `main`:

```
python3 scripts/check_demo_matches_main.py --url http://127.0.0.1:48045 --ref origin/main
→ Máy demo khớp origin/main: 77 route, không thiếu, không thừa.

scripts/e2e_slice.sh --keep (lát cắt dọc trên chính stack này) → 7 pass / 0 fail
```

---

## 3. Bảng 47 hàng, cửa Google, `43dc45a`

Cột **Đo ở đâu** nói thẳng hàng nào tôi tự bấm lượt này (`✅ 31/08 02:3x`), hàng
nào mang từ `qa2-002802` tại `1161570` (`↩ 02802`), hàng nào mang xa hơn từ
`qa-tt-0038` (`↩ 0038`). Hàng mang nguyên là hàng mà `git diff 1161570..43dc45a`
không chạm màn của nó — danh sách 10 file ở mục 1.

| F## | Tên | Nhãn | Đo ở đâu | Bằng chứng |
|---|---|---|---|---|
| F01 | Account Registration | BẤM-ĐƯỢC | ✅ | Cửa Google: chọn người → `PUT 201 /people/{id}` → vào thẳng shell 4 tab |
| F02 | Personal Profile | BẤM-ĐƯỢC | ↩ 02802 | `GET 200 /people/{id}/avatar` + `/posts` + `/finance` |
| F03 | Add Friends | BẤM-ĐƯỢC | ↩ 0038 | Nút "Mở màn kết bạn" trên Cá nhân |
| F04 | Friend Request | BẤM-ĐƯỢC | ↩ 0038 | cùng màn |
| F05 | QR Friend Add | BẤM-ĐƯỢC | ↩ 02802 | Khối "Mã kết bạn của bạn". Quét bằng camera thật: **chưa chứng minh** |
| F06 | Create Group | BẤM-ĐƯỢC | ✅ | `POST 201 /contexts` mỗi lượt vào shell; `[+] → Tạo nhóm` có trong sheet |
| F07 | Group Chat | BẤM-ĐƯỢC | ✅ | Gõ + Gửi ×4 → tin hiện trong luồng, `POST /contexts/{id}/messages` |
| F08 | AI Member | BẤM-ĐƯỢC | ✅ | AI trả lời hai lần: một thẻ lịch trình, một câu hỏi lại về ngân sách 300k |
| F09 | Discover Places | BẤM-ĐƯỢC | ✅ | `GET 200 /places`, 12 chỗ, 5 bộ lọc `role=radio` |
| F10 | Place Detail | BẤM-ĐƯỢC | ↩ 02802 | Địa chỉ, giờ mở, khoảng giá, tag, lý do AI |
| F11 | AI Place Match | BẤM-ĐƯỢC | ✅ | "AI MATCH 96%" · "CHƯA HỢP" · "TẠM HỢP 81%" phân biệt được. Vẫn chấm theo nhóm seed — mục 4 |
| F12 | NL Place Search | BẤM-ĐƯỢC | ↩ 0038 | Ô "Tìm bằng lời" + nút "Tìm bằng AI" |
| F13 | Create Outing | BẤM-ĐƯỢC | ↩ 02802 | `[+] → Tạo chuyến` trong sheet |
| F14 | Invite Members | BẤM-ĐƯỢC | ↩ 02802 | `POST 201 /outings/{id}/invites` → "Đã mời người này vào chuyến" |
| F15 | Outing Timeline | BẤM-ĐƯỢC | ↩ 02802 | 5 chặng + form "Thêm chặng" |
| F16 | AI Itinerary Generator | **BẤM-ĐƯỢC** ⬆ | ✅ | `POST 200 /ai-turn` → `spoke:true`, thẻ 6 chặng, "Xem chi tiết kế hoạch" mở màn đầy đủ |
| F17 | Voting | BẤM-ĐƯỢC | ↩ 02802 | Thẻ bình chọn + "Mở bình chọn mới" |
| F18 | Receipt OCR | BẤM-ĐƯỢC | ↩ 02802 | `POST 200 /receipts/scan`, Gemini đọc 4/4 món |
| F19 | Bill Item Detection | BẤM-ĐƯỢC | ↩ 02802 | 4 dòng tên/SL/thành tiền, sửa tay được |
| F20 | Assign Food To Person | BẤM-ĐƯỢC | ↩ 02802 | Ma trận người × món |
| F21 | AI Person Recognition | KHÔNG-CÓ-ĐƯỜNG | ✅ | `App.tsx:1572` — chỉ `?man=nhan-mat`. Kiểm lại sau #379: vẫn 0 đường trong shell |
| F22 | Visual Food Participation | KHÔNG-CÓ-ĐƯỜNG | ✅ | `App.tsx:1571` — chỉ `?man=mon-cua-toi` |
| F23 | Confidence Score | KHÔNG-CÓ-ĐƯỜNG | ↩ 02802 | Màn nhận diện không in con số tin cậy nào |
| F24 | Expense From Chat | **TẮC** | ✅ | `POST 200 /messages/{id}/expense-draft` đọc đúng "Tiền nướng tối qua · 360.000đ · Người trả: Minh · Người chia: 7 người", rồi "bạn còn phải chốt" và nút duy nhất trên thẻ là **"Đóng"** |
| F25 | Expense From Receipt | BẤM-ĐƯỢC | ↩ 02802 | `POST 201 /expenses` → `confirm` |
| F26 | Expense From Screenshot | BẤM-ĐƯỢC | ↩ 02802 | `POST 200 /screenshots/scan` + "Chốt vào form nhập tay" |
| F27 | Smart Settlement | BẤM-ĐƯỢC | ✅ | Lát cắt dọc trên chính stack này: 7/7 pass, Σ phân bổ = tổng, người ứng tiền không tự nợ mình |
| F28 | Settlement Tracking | BẤM-ĐƯỢC | ✅ | cùng lát cắt: `POST 201 /batches` |
| F29 | Payment Link / QR | BẤM-ĐƯỢC | ✅ | cùng lát cắt: `publish` → envelope + trang khách. **QR chưa quét bằng app ngân hàng thật** |
| F30 | Group Memory | KHÔNG-CÓ-ĐƯỜNG | ↩ 0038 | Không có kho sở thích theo người |
| F31 | Group Preference Profile | KHÔNG-CÓ-ĐƯỜNG | ↩ 0038 | Không màn nào hiện |
| F32 | Proactive Suggestion | KHÔNG-CÓ-ĐƯỜNG | ✅ | `grep -rn "/suggestion" apps/mobile/src` ngoài `api.ts`: 0 call site |
| F33 | Contextual Suggestions | BẤM-ĐƯỢC | ✅ | AI tự hỏi lại "ưu tiên quán nướng hay cafe view đồi?" khi ngân sách không khớp |
| F34 | Budget Awareness | BẤM-ĐƯỢC | ↩ 02802 | "Đã tiêu … / ngân sách …" trên từng chuyến |
| F35 | Group Memory Wall | BẤM-ĐƯỢC | ↩ 02802 | Màn Kỷ niệm + nút "Thêm ảnh". Stack lượt này **0 ảnh** nên không đo lại được phần ảnh |
| F36 | Automatic Trip Album | **BẤM-ĐƯỢC** ⬆ | ✅ | `[+] → Album chuyến đi` → 3 album, tiền đọc từ sổ, mở được từng chuyến |
| F37 | AI Highlight Reel | **BẤM-ĐƯỢC (rỗng)** ⬆ | ✅ | `GET 200 …/reel` → "Chưa dựng được thước phim" vì nhóm 0 ảnh. Thước phim thật: **chưa quét** |
| F38 | Locket Style Widget | KHÔNG-CÓ-ĐƯỜNG | ✅ | `setLuongWidget` chỉ tới từ `moWidgetNgay` = fragment `#vao=widget`; sheet `[+]` sau #379 vẫn không có mục nào |
| F39 | Post | BẤM-ĐƯỢC | ↩ 02802 | "Viết lên tường" trên Cá nhân |
| F40 | Reactions | BẤM-ĐƯỢC | ↩ 02802 | Đếm ♥ trên tường Kỷ niệm |
| F41 | Comments | BẤM-ĐƯỢC | ↩ 0038 | Đếm bình luận hiện trên ảnh; chưa gõ bình luận mới lần nào |
| F42 | Privacy 4 mức | BẤM-ĐƯỢC | ↩ 02802 | Nhãn "Công khai" / "Chỉ mình tôi" |
| F43 | Social Map | **TẮC** | ✅ | `GET 403 /contexts/1aa00000-…/map` → "Bạn không còn trong nhóm này" |
| F44 | Group Heatmap | **TẮC** | ✅ | `GET 403 /contexts/1aa00000-…/heatmap`, cùng lời gọi |
| F45 | Meet-in-the-middle | **TẮC** | ✅ | `GET 200 /areas` → chọn 2 khu → "Tìm chỗ gặp" → `POST 403 /contexts/1aa00000-…/meet` |
| F46 | Group Check-in | BẤM-ĐƯỢC | ↩ 02802 | 5 nút "Đã tới" trên màn chuyến |
| F47 | Automatic Place Detection | KHÔNG-CÓ-ĐƯỜNG | ↩ 0038 | Từ chối có chủ ý |

**Tổng: 35 BẤM-ĐƯỢC · 4 TẮC · 8 KHÔNG-CÓ-ĐƯỜNG.**
Đo lượt này: 21 hàng. Mang từ `qa2-002802`: 19 hàng. Mang từ `qa-tt-0038`: 7 hàng.

Ba hàng tiền — F27 · F28 · F29 — lượt này đo bằng **lát cắt dọc chạy qua
`src/api.ts`**, không phải bằng ngón tay trên trình duyệt. Đường bấm của chúng
được đo ở `qa2-002802`; cái mới lượt này là chúng vẫn đúng trên máy chủ dựng từ
`43dc45a`. Ghi tách ra để không ai đọc `✅` thành "đã bấm lại".

---

## 4. Bốn hàng TẮC còn lại: ba trong số đó là **một** lỗi

F43 · F44 · F45 vẫn chết vì tab Khám phá ghim cứng `context_id=1aa00000-aaaa-…`
(nhóm seed) trong khi phiên đang ở context của chính mình. Lượt này thấy nó ngay
ở lời gọi đầu tiên sau khi vào shell:

```
GET 200 /places?context_id=1aa00000-aaaa-4aaa-8aaa-0000a0000001
```

Hệ quả im lặng vẫn nguyên và vẫn là rủi ro sản phẩm lớn nhất trong bảng này:
**"AI MATCH 96%" đang chấm theo ngân sách và sở thích của một nhóm mà người dùng
không thuộc về.** Ba hàng 403 chỉ là chỗ cái ghim đó lộ ra thành lỗi nhìn thấy được.

F24 là lỗi thứ hai, độc lập: máy chủ đọc đúng khoản chi rồi màn nói "bạn còn phải
chốt" mà không có chỗ chốt.

---

## 5. Ô chưa quét

- **Mã QR chưa được quét bằng app ngân hàng thật.** Chỉ leader đóng được, 15 phút
  với một điện thoại. Mọi assert trong repo chỉ kiểm chuỗi EMVCo và CRC.
- **F37 với ảnh thật** — cả hai stack đều 0 ảnh, nên "AI dựng được thước phim"
  chưa có phép đo nào. Trạng thái rỗng thì đã đo và nó trung thực.
- **F35 phần ảnh** — cùng lý do: stack dùng một lần không có volume ảnh.
- **47 hàng ở cửa số điện thoại** — lượt trước đã chứng minh cửa đó hết tắc ở F07,
  nhưng chưa ai đi đủ 47 hàng qua nó. Vẫn là ô trống.
- **Cửa "Bỏ qua"** (`nguoi = null`) — chưa đo hàng nào.
- **F05 quét mã bằng camera thật · F41 gõ bình luận mới · F12 câu tìm tiếng Việt mới.**
- **Khung 320 / 1440 và chủ đề tối** — lượt này chỉ 390×844 sáng.
- **Ai làm F16 sống lại** — đo hành vi sau bản vá, không chạy A/B với bản trước vá.

## 6. Rủi ro còn mở, theo 5 loại blocker của charter

| # | Loại | Dẫn chứng | Hậu quả | Tiêu chí gỡ chặn |
|---|---|---|---|---|
| 1 | vi phạm spec/cổng | Khám phá ghim `context_id=1aa00000-…`; F43/F44/F45 nhận 403 lượt này | AI MATCH chấm theo nhóm người dùng không thuộc về; 3 tính năng chết ở bước cuối | Khám phá dùng context của phiên; ba route trả dữ liệu |
| 2 | vi phạm spec/cổng | F24: `expense-draft` 200 rồi nút duy nhất là "Đóng" | App bảo "bạn còn phải chốt" và không có chỗ chốt | Thẻ có đường sang form khoản chi |
| 3 | không tái lập được → **đã tái lập** | F21/F22 chỉ tới được bằng `?man=` | Hai tính năng tính vào độ phủ mà người dùng không chạm được | Có nút trong luồng bill, hoặc ghi thẳng là màn cho máy quét |

Cả ba đều là hàng cũ đo lại và vẫn còn; không hàng nào mới sinh ra ở `43dc45a`.

---

## 7. Một việc tôi làm hỏng trên hạ tầng dùng chung — báo thẳng

`scripts/reset_demo_group.py` **bỏ qua** `MOBILE_DATABASE_URL` và mặc định trỏ
vào database **dùng chung** ở `127.0.0.1:5432`. Tôi export biến đó rồi gọi script,
tưởng nó vào stack riêng của mình. Hậu quả trên DB chung:

- nhóm `Team Đà Lạt` (`5cacfdee-…`) bị đổi tên thành
  `Team Đà Lạt (tồn dư 30/08 — KHÔNG dùng để demo)` — **tôi đã đổi lại ngay**,
  kiểm sau khi sửa: `5cacfdee-… → "Team Đà Lạt"`.
- **45 idempotency key của chính fixture seed bị xoá** (script báo
  "key của lane khác 1225 — KHÔNG đụng tới"). Cái này **không khôi phục được**.
  Nó không xoá dòng sổ nào và không đổi dữ liệu demo đang hiển thị; ảnh hưởng duy
  nhất là lần chạy `seed_demo_data.py` tiếp theo trên DB chung sẽ **không** còn
  gặp `422 idempotency_key_reuse` ở 45 key đó.

Ai cần dùng script này trên stack riêng thì phải truyền `--dsn`, không phải biến
môi trường. Đó là cái bẫy, và nó đã cắn thật.

---

## 8. Chạy lại thế nào

```bash
export GEMINI_API_KEY=…                       # từ .env ở gốc repo, không commit
scripts/e2e_slice.sh --keep                   # in ra API + database dùng một lần
python3 scripts/reset_demo_group.py --dsn '<dsn của stack đó>' --yes
MOBILE_SEED_API_BASE_URL=<api> MOBILE_DATABASE_URL=<dsn> python3 scripts/seed_demo_data.py
cd apps/mobile && EXPO_PUBLIC_API_URL=<api> npx expo export --platform web \
  --output-dir /tmp/qa2-dist-sau379 --clear
cd /tmp/qa2-dist-sau379 && python3 -m http.server 8946 --bind 127.0.0.1 &
python3 tests/qa/qa2-do-lai-47/probe_cua_google.py            # câu 1 + canary với --canary
python3 tests/qa/qa2-do-lai-47/probe_album.py                 # F36/F37
python3 tests/qa/qa2-do-lai-47/probe_f16.py                   # F16
python3 tests/qa/qa2-do-lai-47/probe_f24.py                   # F24
python3 tests/qa/qa2-do-lai-47/probe_f45.py                   # F43/F44/F45
```

Cổng và port trong các file probe là hằng số của lượt đo này; đổi stack thì sửa
hai hằng `SITE` và `API` ở đầu file. `probe_f45.py` có thêm một nửa F24 đọc màn
quá sớm (bắt được "Đang đọc…"), nên câu trả lời F24 lấy từ `probe_f24.py` —
file kia giữ nguyên vì đó là cái đã chạy.

## 9. `main` nhích trong lúc đo — kiểm từng commit, không hàng nào đổi

Đo xong ở `43dc45a` thì `origin/main` đã đi tiếp bốn commit. Con số ở trên vẫn
neo vào `43dc45a`; đây là phần kiểm xem nó còn đúng ở đầu `main` hiện tại không.

| commit | Cái gì đổi | Có đổi hàng nào không |
|---|---|---|
| `8dbd772` #389 | Cá nhân: thêm vế "người khác nợ bạn" + route finance | **Không.** F34 vốn đã BẤM-ĐƯỢC; đây là một màn cũ đầy đủ hơn, không phải hàng mới |
| `33d16d8` #395 | Màn 01.03 Cá nhân hóa, đặt **trong `DangKy`** — chỉ file `screens/vao-cua/{CaNhanHoa.tsx,DangKy.tsx,so-thich.ts}` | **Không, ở cửa Google.** Màn đó nằm trên **cửa số điện thoại**; cửa Google (`MoDau` → `ChonNguoi` → shell) không bị chạm. PR cũng tự khai là vỏ: không route nào lưu lựa chọn |
| `b7325d6` #393 | Ca âm cho hai guard | **Không.** Test |
| `62f4ee4` #396 | Tài liệu + probe F26 | **Không.** PR tự ghi "không sửa một dòng mã sản phẩm" |

Nên **35/47 ở cửa Google vẫn là con số của `main` hiện tại**. Nhưng ai đếm ở
**cửa số điện thoại** thì phải đo lại từ đầu: cửa đó vừa mọc thêm một bước.

## 10. Câu không được bỏ

Repo này chưa có bằng chứng hành vi nào (ADR-0006). Bảng trên nói *một máy quét
bấm được bao nhiêu nút và máy chủ trả gì*. Nó không nói người thật hiểu sản phẩm,
và nó không nói mã QR quét được.
