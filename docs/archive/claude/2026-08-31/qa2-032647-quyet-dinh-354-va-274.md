# qa2-032647 — Quyết định cho hai PR cũ: #354 GIỮ (còn 1 việc nhỏ), #274 ĐÓNG (đã bị thay thế)

- **đo tại** `origin/main` = `d752c4c` (kiểm lại còn hiệu lực ở `19eb4a4` — xem mục cuối)
- **protocol_version** v1
- **verdict** — không phải phán quyết PR. Đây là **phiếu quyết định vật cản** Lead yêu cầu:
  nội dung còn đúng không · cần bao nhiêu việc · hay nên đóng.
- **kỹ năng đã dùng** `ai-qa-review` (sáu nhóm mùi test, mục Verification: chạy thật
  rồi mới bình luận, và mutation score là phép đo khách quan thay cho đọc mắt)
- **PR liên quan** #354 (devops, `devops/cong-di-bo-duong-hero` @ `a2bc103`),
  #274 (qa, `qa/tt-0017-cong-drift-271-con-mu` @ `9fb2dc4`)

---

## Kết luận trước, chi tiết sau

| PR | Nội dung còn đúng? | Đề xuất |
|---|---|---|
| **#354** mối nối ảnh → món | **CÒN ĐÚNG** — lỗ vẫn mở nguyên trên main hôm nay | **GIỮ**, trả devops: còn **1 blocker** + rebase 2 dòng |
| **#274** cổng drift của #271 còn mù | **ĐÃ BỊ THAY THẾ** bởi #280 (`0ed541f`) | **ĐÓNG** |

Cả hai đều **không** trùng byte với main (so blob theo đường dẫn: 5/5 và 2/2 file
đều `DIFF`/`MISSING`). Nhưng "không trùng byte" không trả lời được câu hỏi thật —
nên với mỗi cái tôi đo **tính chất** nó khai, chứ không đo đường dẫn.

---

# #274 — ĐÃ BỊ THAY THẾ, đề xuất ĐÓNG

**Một dòng lý do: cả 5 lỗ nó tố cáo đều đã bị #280 bịt, và tôi chứng minh bằng
cách chạy chính bảng đột biến của #274 lên main hôm nay.**

## Vì sao không tin được phép so đường dẫn ở đây

Hai file của #274 (`docs/archive/claude/.../qa-tt-0017-...md`, `tests/qa/qa-tt-0017/mutants.py`)
đều **MISSING** trên main. Đọc thô thì ra "chưa ai làm" — sai. Bản sửa nằm ở
**file khác**: #280 viết lại chính cổng bị tố cáo.

```
git log 6b84f0e..origin/main -- tests/test_postgres_tier_runner.py
-> 0ed541f  Cổng chống drift ngừng đọc chữ và bắt đầu chạy bước workflow (#280)

blob tests/test_postgres_tier_runner.py   lúc #274 đo fde7862  ->  main bd2632a   DIFF
blob .github/workflows/postgres-repository.yml                   SAME (mỏ neo còn nguyên)
```

Mỏ neo `SAME` là điều kiện cần để chạy lại: `mutants.py` **từ chối chạy** (mã 2)
nếu không tìm thấy nguyên văn bước nó định thay — nên nó không thể ra một đột
biến no-op rồi in GREEN.

## Bằng chứng — chạy chính `mutants.py` của #274 trên main `d752c4c`

Cây sạch, worktree riêng (`git worktree add --detach`), chỉ thêm đúng file harness:

```
BASE  -        unmutated tree                    expect GREEN  got GREEN  16 passed in 25.50s
C1    CONTROL  inline pytest, tên runner trong comment (M7)  expect RED  got RED  ok  2 failed, 14 passed
K1    KEEP     vẫn gọi runner, thêm một cờ (M8)              expect GREEN got GREEN ok  16 passed
K2    KEEP     vẫn gọi runner, đổi tên bước (M9)             expect GREEN got GREEN ok  16 passed
E1    EVADE    nối dòng shell `\`                            expect RED  got RED  ok  2 failed, 14 passed
E2    EVADE    cd sâu thêm một cấp                           expect RED  got RED  ok  2 failed, 14 passed
E3    EVADE    tiền tố ./                                    expect RED  got RED  ok  2 failed, 14 passed
E4    EVADE    giữ trong biến shell $TIER                    expect RED  got RED  ok  2 failed, 14 passed
E5    EVADE    KHÔNG CHẠY GÌ CẢ, runner chỉ còn trong comment expect RED got RED  ok  1 failed, 15 passed

ALL ROWS AS EXPECTED -- no evasion shape tried here got through.      EXIT=0
```

Lúc #274 đo (`6b84f0e`): `HOLES: 5 of 5`. Hôm nay: **0 of 5**.

**Vì sao con số XANH này đọc được, chứ không phải một harness đã chết:** hàng
`C1` vẫn **ĐỎ** và `K1`/`K2` vẫn **XANH**. Nếu cổng mới là phép ghim byte trá
hình thì K1/K2 đã đỏ; nếu harness chết thì C1 đã xanh. Ba loại hàng cùng đúng kỳ
vọng mới cho phép đọc E1–E5. Đây đúng là điều `mutants.py` được viết ra để bảo vệ,
và nó bảo vệ luôn cả kết luận đóng PR của chính nó.

## #280 còn để lại bảng thay thế rộng hơn, nên không mất gì khi đóng

```
scripts/dot_bien_cong_drift_hanh_vi.py trên main:  2 CONTROL · 8 KEEP · 10 EVADE
```

10 hàng EVADE là **tập cha** của 5 hàng E1–E5 (#280 ghi rõ trong commit body:
"cổng cũ để lọt 8/9; cổng mới đỏ đúng chỗ ở cả 9"). Giữ `mutants.py` của #274
lại chỉ để có thêm một bản sao hẹp hơn của cùng một phép đo.

## Còn 9 chặng đỏ của #274 thì sao — KHÔNG phải code

```
mobile bundle and tests | failure | steps: 0 | 02:53:38Z -> 02:53:41Z   (3 giây)
shared money format     | failure | steps: 0 | 02:53:38Z -> 02:53:47Z   (9 giây)
api image builds...     | failure | steps: 0 | 02:53:38Z -> 02:53:41Z   (3 giây)
```

`steps: 0` = job **chưa từng khởi động**. Đúng chữ ký billing Lead đã chốt. Không
ai cần sửa gì để "làm xanh" #274 — và đó cũng không phải lý do đóng nó.

> **Đề xuất: ĐÓNG #274.** Lỗ nó báo đã được #280 bịt, đo lại bằng chính harness
> của nó ra 0/5 lọt, và bảng đột biến thay thế trên main rộng hơn.

---

# #354 — CÒN ĐÚNG, giữ lại; còn đúng 1 blocker

## Lỗ nó bịt vẫn mở nguyên trên main hôm nay

Tôi không đọc tiêu đề PR; tôi đo lại từng câu nó khai, trên `origin/main`:

| Nơi | Có cổng gọi? | Đi qua mối nối `scan → readingFromWire → bills`? |
|---|---|---|
| `scripts/gate.sh` `STAGES=(...)` | — | **không có chặng `hero-walk`** trong 17 chặng |
| `tests/qa/qa-tt-0031/di-bo-hero-tren-demo.mjs` | **KHÔNG ai gọi** | có — nhưng là code chết |
| `apps/mobile/tests/e2e/duong-bill.test.mjs` | có (chặng `e2e`) | **không** — 3 chỗ đều bắt đầu từ `toBill()` |
| `apps/mobile/tests/receipt.test.mjs` | có | **không** — 9 chỗ đọc hằng `LIVE_SCAN` đóng băng |
| `apps/mobile/tests/trang-thai.test.mjs:102` | có | **không** — `globalThis.fetch` bị thay bằng stub 422 |

```
git grep -n "di-bo-hero-tren-demo" origin/main -- Makefile scripts .github services apps
-> rỗng
```

Cái duy nhất merge sau đó và **nghe giống** thay thế là #366 (`make demo-key-check`).
Nó không phải. Chính nó tự khai, tôi chạy trên máy demo:

```
ĐẠT — container mobile-local-api-1 (cổng 8099) giữ đúng khoá trong .env, và model API còn nhận khoá đó.
  KHÔNG chứng minh /receipts/scan đọc ra món: khoá sống là điều kiện cần,
  không phải điều kiện đủ. Muốn biết đường hero đi được:  make hero-walk
```

Và Makefile của main viết thẳng ra là nó **bù cho** `hero-walk` chứ không thay:
*"Đó là chỗ nó bù cho `hero-walk`, vốn đắt nên chỉ chạy theo yêu cầu"* — một câu
trỏ tới một mục **chưa tồn tại trên main**.

## Cần bao nhiêu việc để xanh — 3/4 tiêu chí gỡ chặn đã tự đạt

`docs/archive/claude/2026-08-30/phan-quyet-354-cong-hero-walk.md` (đã ở main) ra **FAIL**
với 4 tiêu chí. Tôi đo lại cả bốn trên **cây gộp tôi tự giải** (`a2bc103` ⊕ `d752c4c`):

**1. Bộ đọc bill trên 8099 trả 200 lại — ĐẠT.** Nguyên nhân FAIL hôm qua (502 do
xoay khoá) đã hết. Một lượt đi bộ thật, thư mục phán quyết cách ly:

```
DAT   QUET BILL: anh -> mon (POST /receipts/scan) (6334ms)     <- chính mối nối
      - Cơm tấm sườn nướng x1 = 55000  ... 5 món
DAT   GAN MON: bill + goi y cua AI (POST /bills) (29ms)
...
DAT 16/16 chang — 6.8s     tong bill=235000 dong
```

**2. `dot_bien_hero_walk.py` ra 13/13 mã thoát 0, có C1 XANH — ĐẠT.**

```
ĐẠT A1 readingFromWire trả 0 món           ĐỎ (cần ĐỎ)
ĐẠT A2 tiền không còn là số nguyên đồng    ĐỎ (cần ĐỎ)
ĐẠT A3 scanReceipt gọi sai đường           ĐỎ (cần ĐỎ)
ĐẠT C1 ĐỐI CHỨNG: đổi hình dạng, GIỮ hành vi  XANH (cần XANH)
+ B0–B8 đúng hết
13/13 dòng đúng kỳ vọng.     EXIT=0
```

**3. Rebase, hết xung đột — VIỆC 2 DÒNG.** Xung đột hôm nay vẫn đúng 2 chỗ, và
cả hai là **hợp nhất danh sách**, không ai xoá của ai:

```
Makefile        .PHONY:  main thêm demo-reset/demo-persona-check/demo-key-check
                         PR  thêm hero-walk/hero-walk-status
scripts/gate.sh STAGES:  main thêm `screens` · PR thêm `hero-walk`
```

Giải xong (hợp nhất cả hai), cây gộp sạch:

```
pytest tests/test_gate_covers_every_workflow_job.py -q  ->  6 passed, 16 subtests passed
scripts/repo_guard.py tree HEAD                         ->  passed, 1028 file scan(s)
ruff check (2 file .py của PR)                          ->  All checks passed!
```

**4. Blocker 3 — phán quyết chưa ràng buộc vào code — VẪN MỞ.** Đây là việc thật
còn lại. Tôi không đọc code rồi đoán; tôi đổi đúng một trường rồi chạy cổng:

```
A. phán quyết thật, "sha": "a2bc103"  ->  scripts/gate.sh hero-walk  ->  ĐẠT
B. cùng phán quyết, "sha": "deadbee"  ->  scripts/gate.sh hero-walk  ->  ĐẠT   <- lỗ
```

`deadbee` không tồn tại ở bất kỳ đâu trong repo. Cổng vẫn xanh. Nghĩa là một
lượt đi bộ trên **nhánh khác** vẫn bảo lãnh cho nhánh này — đúng hình dạng
"phán quyết ghi sẵn không ràng buộc vào code". Bảng đột biến của PR có B4
("phán quyết về máy KHÁC" → ĐỎ) nhưng **không có hàng nào cho "phán quyết về
CODE khác"** — nên bảng 13/13 ở trên không hề mâu thuẫn với lỗ này, nó chỉ
không nhìn tới đó.

**Việc còn lại cho devops** (chính phán quyết cũ đã kê đường rẻ):
`--status` so `git merge-base --is-ancestor <sha trong phán quyết> HEAD`; khác
tổ tiên thì mã 2. Cộng **một hàng đột biến mới** (`B9`: phán quyết về code lạ →
phải ĐỎ), nếu không thì lần sau vẫn không ai biết cổng có gác chỗ đó không.
Không tốn thêm lời gọi model nào.

> **Đề xuất: GIỮ #354.** Trả devops làm 2 việc: (a) ràng buộc phán quyết vào sha
> + hàng B9, (b) rebase hợp nhất 2 danh sách. Môi trường đã hết là vật cản.

---

## KHÔNG chứng minh

- **Cây gộp của #354 là cây tôi tự giải, không phải cây devops đã ship.** Mọi số
  ở mục #354 phải đo lại sau khi devops rebase thật.
- **Lượt đi bộ đo máy demo 8099, không đo máy chủ của nhánh #354.** Client dựng
  từ cây gộp, server là container đang chạy — cặp đó là chủ ý của bài đi bộ.
- **Không chạy `make gate` đầy đủ trên cây gộp #354** — chỉ chặng meta-cổng,
  repo guard, ruff, và hai bảng đột biến.
- **Với #274 tôi chỉ chạy `tests/test_postgres_tier_runner.py`** (16 ca) qua
  `mutants.py`, không chạy cả bộ. Đủ để trả lời "5 lỗ còn không", không đủ để
  nói gì về sức khoẻ chung của main.
- **Không đo lại dấu X đỏ trên GitHub của cả hai PR** ngoài một lần đọc
  `steps: 0` để phân biệt billing với code.

## Đo tại `d752c4c`, kiểm lại còn hiệu lực ở `19eb4a4`

main nhích 3 commit giữa lượt (#403, #382, #404). Không commit nào chạm
`scripts/gate.sh`, `Makefile`, `postgres-repository.yml`,
`test_postgres_tier_runner.py`, `dot_bien_cong_drift_hanh_vi.py`,
`duong-bill.test.mjs`, `receipt.test.mjs`, hay `tests/qa/qa-tt-0031/` — nên cả
hai kết luận giữ nguyên. Nếu main đi xa thêm trước khi Lead xử lý, phần đáng
kiểm lại chỉ là tập file đó.

## Dọn dẹp

Worktree tạm `/tmp/qa2-032647-mut274` đã `git worktree remove --force`. Thư mục
phán quyết cách ly `/tmp/qa2-032647-heroverdict` (có lúc chứa sha giả `deadbee`
để đối chứng) đã xoá — không để lại phán quyết nào cho cổng của người khác đọc
nhầm. `.github/workflows/postgres-repository.yml` được `mutants.py` khôi phục
trong `finally`, đã kiểm `git status --porcelain .github/` ra rỗng.
