# Generated by scripts/render_parity_422_scenarios.py inside the pinned API image;
# edit the generator, not this file. Requests only: the expected answer is
# whatever the Python reference returns (ADR-0029 §2.4, ADR-0010 §6.1).
# Route: GET /people/me
# Body: none declared
# Query: none. Actor header: X-Actor-ID via get_actor.
id: generated/w10-422/get-people-me
routes: ['GET /people/me']
auth_mode: dev
personas:
  owner: {}
steps:
  # no path, query or body parameters to validate
  - id: owner_plain
    as: owner
    request:
      method: GET
      path: '/people/me'
  - id: anonymous_plain
    as: anonymous
    request:
      method: GET
      path: '/people/me'
  # inputs this route does not declare are ignored by FastAPI
  - id: undeclared_query_parameter
    as: owner
    request:
      method: GET
      path: '/people/me?unknown_field=x'
  - id: undeclared_malformed_json_body
    as: owner
    request:
      method: GET
      path: '/people/me'
      headers:
        content-type: 'application/json'
      body_raw: '{"x": '
  # precedence: the actor dependency before path validation
  - id: bad_actor_id_valid_path
    as: owner
    request:
      method: GET
      path: '/people/me'
      headers:
        x-actor-id: 'khong-phai-uuid'
