/** Fully visible small rows qualify; a tall row must cover 60% of the viewport. */
export const CHAT_VIEWABILITY = Object.freeze({
  viewAreaCoveragePercentThreshold: 60,
  minimumViewTime: 600,
});
