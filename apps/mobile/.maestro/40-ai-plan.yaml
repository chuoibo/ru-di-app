# AI thật (V4, M3): `/plan` trong chat trên API có khoá Gemini → một thẻ của Rủ Đi
# AI xuất hiện trong cuộc trò chuyện (text, gợi ý địa điểm hoặc lịch trình), KHÔNG
# phải câu im lặng «chưa nối được mô hình». Chỉ chạy ở `--otp --ai`, sau khi
# harness đã kiểm khoá còn sống bằng scripts/check_demo_ai_key.py (khoá chết là
# ĐỎ, không phải bỏ qua). Chạy sau flow 25 trong cùng lượt; tự tạo nhóm «Plan QA»
# để thẻ AI đầu tiên trong chat là của /plan.
# Harness kiểm máy chủ sau flow (scripts/kiem_the_ai_sau_40.py): câu trả lời
# ai_card không tác giả — từ lát 7 là tin trả lời `tra_loi` vào tin /plan, các
# thẻ text|places|itinerary nằm trong payload.phan — và mọi place_id trong mọi
# phần nằm trong GET /places (grounded).
appId: com.lakiet.rudi
---
- stopApp
- launchApp
- runFlow: _bo-qua-dev-menu.yaml
- extendedWaitUntil:
    visible: "Rủ Đi thôi!|Khám phá|Chưa có nhóm nào"
    timeout: 150000
# Giữ phiên đang sống của flow trước (C, D đều là thành viên «Hoi QA»): xin mã
# lại trong nhịp 60 s sẽ bị máy chủ từ chối. Chỉ đăng nhập (bằng số D, số mà
# harness đã xin mã cách đây lâu) khi app đang ở màn chào.
- runFlow:
    when:
      visible: "Rủ Đi thôi!"
    file: _dang-nhap-d.yaml
- extendedWaitUntil:
    visible: "Khám phá"
    timeout: 60000
- tapOn: "Tin nhắn"
- extendedWaitUntil:
    visible: "Hoi QA"
    timeout: 60000
# Một nhóm MỚI, chưa có thẻ AI nào: thẻ «Rủ Đi AI…» đầu tiên trong chat này chắc
# chắn là câu trả lời cho /plan, không phải thẻ của flow 30 còn trên màn.
- tapOn: "Tạo nhóm"
- extendedWaitUntil:
    visible: "Đặt tên cho hội"
    timeout: 30000
- tapOn: "Ô tên nhóm"
- inputText: "Plan QA"
- hideKeyboard
- tapOn: "Mở nhóm"
- extendedWaitUntil:
    visible: "Khám phá"
    timeout: 60000
- tapOn: "Tin nhắn"
- extendedWaitUntil:
    visible: "Plan QA"
    timeout: 60000
- tapOn: "Plan QA"
- extendedWaitUntil:
    visible: "Ô soạn tin"
    timeout: 60000
- assertNotVisible: "Rủ Đi AI.*"
- tapOn: "Ô soạn tin"
- inputText: "/plan toi nay ca hoi di an o Da Lat, ngan sach vua"
- tapOn: "Gửi tin nhắn"
# Mô hình cần thời gian; 120 s là trần cho một lượt. Câu trả lời trong luồng ký
# chân «Rủ Đi AI · đọc N tin» (hoặc «· chỉ đọc lời nhờ»).
- extendedWaitUntil:
    visible: "Rủ Đi AI · (đọc [0-9]+ tin|chỉ đọc lời nhờ)|Rủ Đi AI|Rủ Đi AI gợi ý|Rủ Đi AI phác lịch trình"
    timeout: 120000
- assertNotVisible: "Rủ Đi AI chưa nhận lời nhờ"
- assertNotVisible: "Rủ Đi AI chưa nối được mô hình trên máy chủ này."
- assertNotVisible: ".*đang im lặng.*"
- hideKeyboard
- takeScreenshot: 40-ai-tra-loi
