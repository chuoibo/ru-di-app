# Pinned so CI and a developer machine resolve the same tree. The application
# dependencies live in pyproject.toml; this file exists because CI needs the
# test and migration tooling too, and an unpinned CI is a test suite that can
# start failing for reasons nobody changed.
alembic==1.14.0
fastapi==0.115.6
# ADR-0029: the parity harness compares Go with this exact Python stack, and
# pydantic, pydantic-core and starlette decide the bytes of every 422, 404, 405
# and 307 body. Unpinned, the image took whatever was newest on build day, so two
# builds of one commit could disagree on error text. These are the versions the
# image resolved on 2026-09-14 (image sha256:c707ef4a9cf4), pinned as they were.
pydantic==2.13.5
pydantic-core==2.46.5
starlette==0.41.3
anyio==4.15.0
annotated-types==0.8.0
typing-inspection==0.4.4
typing-extensions==4.16.0
markupsafe==3.0.3
h11==0.16.0
google-genai==2.10.0
google-auth==2.55.1
httpx==0.28.1
jinja2==3.1.5
# The upload sanitiser strips every byte of metadata before an image touches
# disk. It was declared only in pyproject.toml, which CI never installs, so
# tests/media died at collection on the runner while staying green here -- this
# machine had Pillow pulled in by something unrelated.
pillow==12.2.0
# F22 runs its face cascade in-process; nothing about a group photo leaves
# the box. This pin is what the container gets. Note the skew it creates:
# a dev machine may already carry a different OpenCV (this one has 4.13.0
# via conda), and the two can disagree about how many faces are in a
# picture. Nothing in the suite depends on the count -- the API tier stubs
# the detector and tests/live asserts a floor of one, not an exact number.
opencv-python-headless==4.14.0.94
psycopg[binary]==3.2.3
# FastAPI needs this for the guest payment-report form. It was missing, and
# the suite passed locally only because another package had pulled it in.
python-multipart==0.0.20
pytest==8.3.4
pytest-subtests==0.14.1
ruff==0.9.2
segno==1.6.1
sqlalchemy==2.0.36
uvicorn[standard]==0.34.0
